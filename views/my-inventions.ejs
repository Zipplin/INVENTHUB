<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Inventions</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<div class="app-shell">

    <aside class="sidebar">

        <div class="sidebar-logo">💡 InventHub</div>

        <nav class="sidebar-nav">
            <a href="/dashboard">🏠 Dashboard</a>
            <a href="/my-inventions" class="active">💡 My Inventions</a>
            <a href="/exhibitions">🏆 Exhibitions</a>
            <a href="/marketplace">🛒 Marketplace</a>
            <a href="/bookmarks">⭐ Bookmarks</a>
            <a href="/conversations">💬 Messages</a>
            <a href="/notifications">🔔 Notifications</a>
        </nav>

        <div class="sidebar-bottom">
            <a href="/upload.html">➕ Upload Invention</a>
            <a href="/logout">🚪 Logout</a>
        </div>

    </aside>

    <main class="main">

        <h1 class="page-heading">📁 My Inventions</h1>
        <p class="page-subheading">Welcome, <%= fullname %> 👋</p>

        <% if (inventions.length === 0) { %>

            <div class="feed-card" style="padding: 40px 20px; text-align:center;">
                <p style="color:var(--text-muted); margin:0 0 16px;">
                    You haven't uploaded any inventions yet.
                </p>
                <a href="/upload.html" class="btn btn-primary">➕ Upload Your First Invention</a>
            </div>

        <% } else { %>

            <% inventions.forEach(function(invention) { %>

                <div class="feed-card">

                    <% if (invention.image) { %>
                        <img src="/uploads/<%= invention.image %>" class="feed-card-image" alt="Invention image">
                    <% } %>

                    <div class="feed-card-body">

                        <h2 class="invention-title"><%= invention.title %></h2>

                        <div class="spec-tags">
                            <span class="spec-tag"><%= invention.category %></span>
                            <span class="spec-tag"><%= invention.industry %></span>
                            <span class="spec-tag"><%= invention.country %></span>
                            <span class="spec-tag">STAGE: <%= invention.stage %></span>
                        </div>

                        <p class="invention-description"><%= invention.description %></p>

                        <div class="meta-row">
                            <span>🤝 Looking for: <%= invention.lookingFor %></span>
                        </div>

                        <div class="action-bar">

                            <a href="/edit/<%= invention.id %>" class="btn btn-outline">✏️ Edit</a>

                            <form action="/delete/<%= invention.id %>" method="POST" style="display:inline;">
                                <button
                                    type="submit"
                                    class="btn btn-danger"
                                    onclick="return confirm('Delete this invention?')">
                                    🗑️ Delete
                                </button>
                            </form>

                        </div>

                    </div>

                </div>

            <% }) %>

        <% } %>

    </main>

</div>

</body>
</html>